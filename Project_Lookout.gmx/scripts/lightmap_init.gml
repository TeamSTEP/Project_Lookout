///lightmap_init(surface_width, syrface_height, brightness)
//create a surface with the provided width and height
lightmap=surface_create(argument0,argument1);

//set the brightness to the 3rd argument
//the higher the number is, the brighter it gets
brightness=argument2;
